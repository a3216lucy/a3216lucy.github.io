<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>【心得分享】寫在活動之後</title>
    <url>/2021/09/30/pm361_activity/</url>
    <content><![CDATA[<p>身為活動的 PO，很高興能夠邀請到硬體業 PM Eva 和軟體業 PM Rafeni 來和大家分享他們的經歷。</p>
<p><img src="https://miro.medium.com/max/1400/1*oEIRVqvHSzxU-h9ugsPSKQ.png"></p>
<p style="text-align:center"><i>活動 ：PM 相談室《新手篇》</i></p>

<blockquote>
<h2 id="前情提要"><a href="#前情提要" class="headerlink" title="前情提要"></a>前情提要</h2><p>筆者隸屬於 PM361 社群，近期於 2021/09 月時舉辦了「PM 相談室《新手篇》」活動，主要的 TA 為那些想要轉職 PM 或剛入行的新手 PM 們。<br>我們希望能夠透過有經驗者的分享，來幫助他們解決一些常見的疑惑、瞭解 PM 的工作職務，因而對 PM 的認知有個基礎的架構。</p>
</blockquote>
<br>

<p>因為在活動中的身份不太一樣，此篇文章主要會以「籌劃者」的角度來回顧活動，而非分享活動心得體驗，大綱可分為以下幾個部分：</p>
<ul>
<li>活動的初衷</li>
<li>主題挑選與聚焦</li>
<li>活動檢討與回饋</li>
</ul>
<br>

<h2 id="活動的初衷"><a href="#活動的初衷" class="headerlink" title="活動的初衷"></a>活動的初衷</h2><p>說到 PM 361 社群 ，社團成立的衷旨為「期許 PM 們擁有 360 ° 的全方位視野，加上 1° 自己的觀點」，讓同為 PM 的這群同溫層有個平台可以互相交流、成長。而在我加入社群後，也一直為著這個目標努力著，和成員們除了透過每月舉辦的活動，分享 PM 相關知識與議題，也不斷在關注著社團內的需求與動向。</p>
<p>在分析社團成員的組成與面向後，我們發現主要的 TA 大多為：</p>
<ul>
<li>對 PM 有興趣、欲轉職為 PM 者（本身非 PM 或為新鮮人）</li>
<li>0 - 3 年的新手 PM</li>
</ul>
<p>其分別的痛點大致如下：</p>
<h3 id="對-PM-有興趣、欲轉職為-PM-者"><a href="#對-PM-有興趣、欲轉職為-PM-者" class="headerlink" title="對 PM 有興趣、欲轉職為 PM 者"></a>對 PM 有興趣、欲轉職為 PM 者</h3><ul>
<li>不清楚 PM 的工作職能</li>
<li>找不到 PM 前輩可以請教</li>
<li>不知道自己是否適合、能否勝任這個職位</li>
<li>不知道特定產業的 PM 差別</li>
<li>有薪資上的需求（高薪）</li>
</ul>
<h3 id="0-3-年的新手-PM"><a href="#0-3-年的新手-PM" class="headerlink" title="0 - 3 年的新手 PM"></a>0 - 3 年的新手 PM</h3><ul>
<li>對產品策略與規劃不熟悉，沒有好的思考框架</li>
<li>溝通問題（向上管理、跨部門協作）</li>
<li>無系統化學習（學習管道、專業技能）</li>
<li>不知道如何提升工作效率（工具、方法模型的使用）</li>
<li>職涯規劃（履歷、面試技巧）</li>
</ul>
<p>為了滿足上述的需求，才規劃了此次的活動。</p>
<br>

<h2 id="主題挑選與聚焦"><a href="#主題挑選與聚焦" class="headerlink" title="主題挑選與聚焦"></a>主題挑選與聚焦</h2><p>在確定了活動的 TA 與大方向後，我首先針對「產業別」來挑選適合的講師，最終鎖定「硬體業」和「軟體業」。<br>除了希望能對比做出差異化之外，這兩個也算是市場普遍的兩大產業。（當然未來有機會的話，也可以瞄準電商、醫療等領域）</p>
<p>起初設定的分享內容會相對深一點，適合有一些基礎的對象參與，不過在瀏覽報名者的回饋與疑惑後，發現普遍都對 PM 沒概念，才調整以「知識普及」、「觀念建立」、「框架分享」為導向，列出幾個關鍵標籤：</p>
<ul>
<li>轉職／求職</li>
<li>PM 工作職能介紹</li>
<li>溝通與協作</li>
</ul>
<p>也就是大家現在看到的版本，而過程中也和講師們協調過多次，最終才定稿。<br>P.S. 真的特別感謝講師們的配合，花了很多時間討論與修改內容，真的是人美又心善，辛苦了！！</p>
<br>

<h2 id="活動檢討與回饋"><a href="#活動檢討與回饋" class="headerlink" title="活動檢討與回饋"></a>活動檢討與回饋</h2><p>接著來談談活動後，回顧整場活動：</p>
<h3 id="1-參與度"><a href="#1-參與度" class="headerlink" title="1. 參與度"></a>1. 參與度</h3><p>從出席率和活動過程來看，與會者的參與度還算高，都蠻認真的在聽講師分享，不過因爲此場活動沒有開放「現場互動」，而是採用「單方面文字回饋」，無法反應「活躍度」與「互動率」，這點確實蠻可惜的。</p>
<h3 id="2-控場與節奏"><a href="#2-控場與節奏" class="headerlink" title="2. 控場與節奏"></a>2. 控場與節奏</h3><p>身為 PO 兼主持人，我必須先承認在節奏與氛圍上，有很多地方需要再改進，流程、平台工具、主持切換、背景音樂等部分都有優化的空間，之後會再重點留意。</p>
<h3 id="3-參與者回饋"><a href="#3-參與者回饋" class="headerlink" title="3. 參與者回饋"></a>3. 參與者回饋</h3><p>根據參與者填答的回饋內容，針對「框架」、「心法」、「案例分享」等內容頗受好評，也有不少人反映後續可以延續此次活動主題，多邀請不同產業的 PM 來做經驗分享，這方面我們會納入考慮，作為下次活動的備選清單。</p>
<br>
<hr>

<p>綜上所述，為我個人對此次活動的淺見，希望有參與活動的大家都有所收穫！<br>如果有任何建議與指正，也歡迎留言告訴我！</p>
]]></content>
      <tags>
        <tag>PM</tag>
        <tag>心得</tag>
      </tags>
  </entry>
  <entry>
    <title>JS 宣告變數： var／ let / const 差異</title>
    <url>/2021/10/10/var-const/</url>
    <content><![CDATA[<p>如果你像我一樣開始學 JS，多少會有個疑惑：學習時是用 let / const 宣告變數，怎麼網路上查詢的 JS 程式碼，有些是用 var 宣告變數的呢？它們之間有什麼差異？<br>因好奇驅使，於是誕生這篇談 「 var 」 與 「 let / const 」 幾項差異的文，大綱如下：</p>
<ul>
<li>ES6 後，從 var 到 let / const</li>
<li>var 是函式作用域，let / const 是區塊作用域</li>
<li>var 與 let ，for 迴圈的綁定（bind）差異</li>
<li>var 的提升與 let / const 不同</li>
<li>var 允許重複宣告，let / const 會出錯</li>
</ul>
<p>p.s. 第三部分 for loop 是我覺得最有趣且難解釋的，推薦閱讀＆討論 !</p>
<hr>
<br>

<h2 id="ES6-後，從-var-到-let-const"><a href="#ES6-後，從-var-到-let-const" class="headerlink" title="ES6 後，從 var 到 let / const"></a>ES6 後，從 var 到 let / const</h2><p>為何現在 (2019) 學習的是 <code>let / const</code> ，卻在網上看到 <code>var</code> 宣告變數 ？</p>
<p>因為目前 JavaScript 主流是 ES6 版本，在這個版本中，是推薦使用 let 和 const 宣告變數，而在網上查詢的資料，很可能是 ES6 版本前撰寫的程式碼，那時候宣告變數的方式還是用 var 語法。</p>
<p>總言之，在 ES6 版本後，會推薦用 let 與 const ，而非 var 宣告變數（原因本文會有說明）。</p>
<p>而 var 與 let / const ，主要有幾項差異：</p>
<ul>
<li>作用域 (scope) 不同</li>
<li>for 迴圈的綁定 (bind) 差異</li>
<li>變量提升 (hoisting) 不同</li>
<li>重複宣告的差異</li>
</ul>
<p>這些改變的主要好處是： <em>讓 JS 變數的操作更加嚴謹</em>，減少不直覺、出錯或重複宣告的可能性，藉此讓多人或個人大型開發更順利。接下來將一一討論及舉例，文中是我理解後的解釋脈絡，如有誤，歡迎不吝指正。</p>
<br>

<h2 id="var-是函式作用域，let-const-是區塊作用域"><a href="#var-是函式作用域，let-const-是區塊作用域" class="headerlink" title="var 是函式作用域，let / const 是區塊作用域"></a>var 是函式作用域，let / const 是區塊作用域</h2><p>在 ES6 前，並沒有區塊作用域（block scope）的概念，僅有全域（global scope）與函式作用域（function scope），所以 var 宣告的變數，具有函式作用域（function scope）的性質，代表切分最小單位的有效範圍是 function。</p>
<p>直到 ES6 後，新增區塊切分的概念， let / const 宣告的變數，才具有區塊作用域（block scope）的性質，切分最小單位的有效範圍是 blcok。</p>
<p>而所謂的作用域就是 <em>「變數有效的作用範圍」</em>，最大為全域作用範圍，意指變數有效範圍是全部範圍。而上文所稱的區塊是指 { }大括號範圍 。</p>
<blockquote>
<h3 id="綜合整理："><a href="#綜合整理：" class="headerlink" title="綜合整理："></a>綜合整理：</h3><ul>
<li>var 具有「函式作用域」，亦即在函式中宣告的變數，有效作用範圍會被限制在該函式中。但不具有區塊作用域，所以在區塊中宣告的變數，依然會作用到區塊之外，並不會被區塊限制住。</li>
<li>let / const 具有「區塊作用域」，亦即在區塊中宣告的變數，有效作用範圍會被限制在該區塊中。</li>
</ul>
</blockquote>
<p>看例子更好懂，在區塊 { } 中，用 var / let，宣告柯基犬的名字為吐司來看看結果。</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line"><span class="regexp">//</span><span class="regexp">/ 「var」 不受區塊限制，區塊外變數存取成功。/</span><span class="regexp">//</span></span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">  var corgiDogName = <span class="string">&#x27;吐司&#x27;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">console.log(corgiDogName);</span><br><span class="line"><span class="regexp">//</span> 吐司</span><br><span class="line"></span><br><span class="line"><span class="regexp">//</span><span class="regexp">/「let」會受區塊限制，區塊外變數存取失敗。/</span><span class="regexp">//</span></span><br><span class="line">&#123;</span><br><span class="line">  let corgiDogName = <span class="string">&#x27;吐司&#x27;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">console.log(corgiDogName);</span><br><span class="line"><span class="regexp">//</span> ReferenceError: corgiName is not defined</span><br></pre></td></tr></table></figure>

<p>雖然 var 不受區塊限制，但會受到函式範圍限制，如下:</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line"><span class="regexp">//</span><span class="regexp">/ 「var」 受函式限制，函式外變數存取失敗。/</span><span class="regexp">//</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> callCorgi() &#123;</span><br><span class="line">  var corgiDogName = <span class="string">&#x27;吐司&#x27;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">console.log(corgiDogName);</span><br><span class="line"><span class="regexp">//</span> ReferenceError: corgiDogName is not defined</span><br><span class="line"></span><br><span class="line"><span class="regexp">//</span><span class="regexp">/ 即使用 var 宣告相同的變數名稱 dogName，但因為「 函式作用域 」，所以不會讓同名變數衝突。 /</span><span class="regexp">//</span></span><br><span class="line"></span><br><span class="line"><span class="regexp">//</span> 將柯基犬命名為吐司，之後用 callCargi() 呼叫</span><br><span class="line"><span class="keyword">function</span> callCargi() &#123;</span><br><span class="line">  var dogName = <span class="string">&#x27;吐司&#x27;</span>;</span><br><span class="line">  console.log(dogName);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="regexp">//</span> 將米克斯犬命名為厚片，之後用 callMix() 呼叫</span><br><span class="line"><span class="keyword">function</span> callMix() &#123;</span><br><span class="line">  var dogName = <span class="string">&#x27;厚片&#x27;</span>;</span><br><span class="line">  console.log(dogName);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">callCargi(); <span class="regexp">//</span> 吐司</span><br><span class="line">callMix(); <span class="regexp">//</span> 厚片</span><br></pre></td></tr></table></figure>

<p>由上述範例，可以看出限定作用域範圍有兩個好處：</p>
<ul>
<li>避免同名變數的衝突。</li>
<li>能維持最小權限的原則，以避免變數資料被不當存取。</li>
</ul>
<p>而過往用 var 宣告，僅有 function 函式範圍有這樣的好處，而如今用 let / const 宣告，就能讓上述好處發生在 if、for 等「 區塊作用域 」當中，藉此降低多人協作或大型專案的衝突情況。</p>
<br>
<hr>

<h2 id="var-與-let-，for-迴圈的綁定（bind）差異"><a href="#var-與-let-，for-迴圈的綁定（bind）差異" class="headerlink" title="var 與 let ，for 迴圈的綁定（bind）差異"></a>var 與 let ，for 迴圈的綁定（bind）差異</h2><p>讓我們用一個經典案例作為第二部分的開場。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 用 for 迴圈執行五次，每隔 0.1 秒會印出 i ///</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; <span class="number">3</span>; i++) &#123;</span><br><span class="line">  <span class="built_in">setTimeout</span>(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(i);</span><br><span class="line">  &#125;, <span class="number">100</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>你覺得上述的程式執行的結果為何？</p>
<p>（Ａ） 0 、 1 、 2 （Ｂ） 3 、 3 、 3</p>
<p>是不是 A 啊 ～ No ～ No ～ No ～ 其實是 B 哦。</p>
<br>

<p>我想很多沒學過 var 的人，包括我，都會直覺性的選擇 A ，因為如果我們學的是 let ，而將 var 替換成 let ，答案就會是 A ，這不是更符合 for 的效用嗎！！！ 可惜事與願違，真正執行的結果會是 B。</p>
<p>上述預期結果應該是 0 1 2 ，卻莫名變成 3 3 3，正與宣告變數語法 var / let 有關。究竟怎麼了？</p>
<p>要解釋這個問題，要分成兩點討論：</p>
<ol>
<li>setTimeout() 的時間延遲，與 function 中 console.log(i) 的「 執行時間點 」。</li>
<li>function 中 console.log(i) 的「 值怎麼來 」的。</li>
</ol>
<p>首先來看第一點，當進入 for 迴圈時，宣告變數 <code>var i = 0</code> ，並開始條件判斷，當 <code>i &lt; 3 </code>時， <code>i + 1</code>，執行完後，接著需要等待 0.1 秒的時間，才會進行 <code>setTimeout()</code> 內的 <code>function() &#123; console.log( i ) ; &#125;</code>。</p>
<p>而 JavaScript 是「異步/非同步」語言，因此在等待執行 <code>function() &#123; console.log( i ) ; &#125;</code> 前的這 0.1 秒內，會先執行完已經能執行的 for 迴圈。</p>
<p>所以現在能理解第一點的結論： <code>function</code> 中，<code>console.log( i )</code> 的執行時間點會在 for 迴圈執行完畢之後。</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line"><span class="regexp">//</span><span class="regexp">/因為非同步與延遲時間，所以會先執行完 for 迴圈後，才執行 function。/</span><span class="regexp">//</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (var i = <span class="number">0</span>; i &lt; <span class="number">3</span>; i++) &#123;</span><br><span class="line">  <span class="regexp">//</span> <span class="keyword">for</span> 迴圈正在 murmur : 還要等你 setTimeout <span class="number">0.1</span> 秒，不如我先做一做，不要浪費時間。</span><br><span class="line">  setTimeout(<span class="keyword">function</span> () &#123;</span><br><span class="line">    console.log(i);</span><br><span class="line">  &#125;, <span class="number">100</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="regexp">//</span> <span class="number">3</span> <span class="number">3</span> <span class="number">3</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>澄清一下，目前為止所提的第一點，其實與 var 和 let 的差異是沒關係的，只是說明非同步和時間點的狀況。接著進入到第二點，console.log(i) 的值是怎麼來的，這就與 var 和 let 的差異有關了。</p>
<p>var 是函式作用域，這段 for 迴圈程式碼外沒有任何包覆，所以 var 所宣告的 i 會存在於全域 window (瀏覽器) 當中，且只會被綁定（bind）一次，也可以說是共用一個 instance。</p>
<p>加上 for 迴圈會先跑完，才 console.log( i )，所以最後 console.log( i ) 的值會才會是 3。</p>
<p>至於 let 則是「 區塊作用域 」每次 i 都會被紀錄在創造出來的區域中，更精確地說，是每次迭代都會建立一個新的環境（context），而這個環境會紀錄當下的變數 i 值，不會覆蓋掉上一個環境裡面的變數值，因此可以產生多個 i 值。。</p>
<p>我有做張示意圖，便於理解「整體概念」，但細節上可能有出入。</p>
<p><img src="https://i.imgur.com/PQ05GIL.png"></p>
<p>讓們適時地整理一下，在 for 迴圈中，</p>
<ul>
<li>var 的情況只會綁定一次，而且不具區塊作用域，最終會只有一個值存在於全域中（以此例而言），也可以說只有一個 instance。</li>
<li>let 的情況會發生重複綁定，而且具有區塊作用域，或者說多個紀錄變數的環境，最終會有多的值存在於 for 迴圈區塊中，也可以說會有多個 instance。</li>
</ul>
<p>就實作而言，雖然僅有 var 的情況下也可以用「立即呼叫執行函式 IIFE」處理這樣的狀況，但較為複雜且不直覺，改用 let 後，就能簡單處理，且能更直覺地知道 for 迴圈結果啦！</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">///將 var i 改為 let i 後，輕易解決問題。///</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; <span class="number">3</span>; i++) &#123;</span><br><span class="line">  <span class="built_in">setTimeout</span>(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(i);</span><br><span class="line">  &#125;, <span class="number">100</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 0 1 2</span></span><br><span class="line"></span><br><span class="line"><span class="comment">///不改 var i 的情況下，以 IIFE 相對複雜且不直覺。///</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; <span class="number">3</span>; i++) &#123;</span><br><span class="line">  (<span class="function"><span class="keyword">function</span> (<span class="params">x</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">setTimeout</span>(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(x);</span><br><span class="line">    &#125;, <span class="number">100</span> * x);</span><br><span class="line">  &#125;)(i);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 0 1 2</span></span><br></pre></td></tr></table></figure>

<p>呼，接下來進入倒數第二 part，剩下的 part 字數較少 ， 你就快看完了！</p>
<br>
<hr>

<h2 id="var-的提升與-let-const-不同"><a href="#var-的提升與-let-const-不同" class="headerlink" title="var 的提升與 let / const 不同"></a>var 的提升與 let / const 不同</h2><p>讓我們再次以一個問題開始 :</p>
<figure class="highlight abnf"><table><tr><td class="code"><pre><span class="line">console.log(i)<span class="comment">;</span></span><br><span class="line">var i = <span class="number">5</span><span class="comment">;</span></span><br></pre></td></tr></table></figure>

<p>你覺得上述的程式執行的結果為何？</p>
<ul>
<li>（Ａ） 5</li>
<li>（Ｂ） ReferenceError</li>
<li>（Ｃ） undefined</li>
</ul>
<p>可以想一下 XD</p>
<p>如果你跟我一樣比較少使用 var ，常使用 let 的話，應該會認為是（Ｂ） ReferenceError。當然，不會那麼容易，印出來的結果其實是 （Ｃ） undefined。</p>
<p>非常不直覺啊…，因為 undefined 代表找不到值，已經被宣告，但尚未賦值，而 ReferenceError 則表示根本找不到 i ，尚未宣告。所以一般而言，會預期上述程式碼結果應該為「尚未宣告變數的ReferenceError」。</p>
<p>但以結果來說，卻是印出 undefined ，即是代表：雖然我們看不見，但其實在 <code>console.log(i)</code> 之前，i 就已經被宣告了，只是尚未賦值。</p>
<p>是不是頗違反直覺，因為 console.log(i) 前，根本沒有程式碼…。上述狀況，我們可以想像成下面這段程式碼來表示：</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line"><span class="regexp">//</span><span class="regexp">/由於 var 直接變量提升，所以上面程式碼等同下面程式碼/</span><span class="regexp">//</span></span><br><span class="line"></span><br><span class="line">console.log(i);</span><br><span class="line">var i = <span class="number">5</span>;</span><br><span class="line"><span class="regexp">//u</span>ndefined</span><br><span class="line"></span><br><span class="line">var i;</span><br><span class="line">console.log(i);</span><br><span class="line">i = <span class="number">5</span>;</span><br><span class="line"><span class="regexp">//u</span>ndefined</span><br></pre></td></tr></table></figure>

<p>這其中是因為 var 有著「提升（hoisting）」的特性，其實不僅是 var ，function 也有這個特性。以 var 宣告變數而言，「 變數提升 」簡而言之是: 在執行任何程式碼前，會把變數放進記憶體中，這樣的特點是，可以在程式碼宣告該變數之前使用它。</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line"><span class="regexp">//</span><span class="regexp">/ 因為 hoisting，所以可以在宣告變數前，就預先使用變數，所以可以寫完後一起宣告/</span><span class="regexp">//</span></span><br><span class="line"></span><br><span class="line">i = <span class="number">2</span>;</span><br><span class="line">n = <span class="number">3</span>;</span><br><span class="line">console.log(i + n);</span><br><span class="line">var i;</span><br><span class="line">var n;</span><br><span class="line"><span class="regexp">//</span> <span class="number">5</span></span><br></pre></td></tr></table></figure>

<p>在這樣的情況下，只需要有賦值就不會出錯，即使尚未宣告變數也不會出錯。</p>
<p>這樣會造成什麼問題？當養成了「 後宣告 」的習慣，如果最後忘了用 var 宣告呢？並不會出錯，只是變數會跑到全域中，變成全域變數，可能造成些 bug，像是：</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line"><span class="regexp">//</span><span class="regexp">/ 函式中沒有用 var 宣告，導致污染到全域 /</span><span class="regexp">//</span></span><br><span class="line"></span><br><span class="line">var x = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> addFunc(y) &#123;</span><br><span class="line">  x = <span class="number">100</span>;</span><br><span class="line">  x = x + y;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">addFunc(<span class="number">50</span>);</span><br><span class="line">console.log(x);</span><br><span class="line"><span class="regexp">//</span> <span class="number">150</span>，預期應該要是 <span class="number">1</span>，但函式中的 x 跑出來了</span><br></pre></td></tr></table></figure>

<p>而在 let 中呢，相對的 hoisting 是較安全的（ let 還是有 hoisting，只是情況不同，可以搜尋關鍵字 TDZ ），所以 習慣用 let 的開發者，通常會先宣告變數，而不會習慣先運算變數後宣告的情況，藉此降低開發出錯的機率 。</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line"><span class="regexp">//</span><span class="regexp">/ 先使用變數，後宣告會直接「出錯」，更嚴謹的養成先宣告，後使用/</span><span class="regexp">//</span></span><br><span class="line"></span><br><span class="line">console.log(i);</span><br><span class="line">let i = <span class="number">5</span>;</span><br><span class="line"><span class="regexp">//</span>ReferenceError</span><br><span class="line"></span><br><span class="line">i = <span class="number">5</span>;</span><br><span class="line">console.log(i);</span><br><span class="line">let i;</span><br><span class="line"><span class="regexp">//</span>ReferenceError</span><br></pre></td></tr></table></figure>

<p>這樣的使用方式除了降低錯誤外，也相對直覺。</p>
<br>
<hr>

<h2 id="var-允許重複宣告，let-const-會出錯"><a href="#var-允許重複宣告，let-const-會出錯" class="headerlink" title="var 允許重複宣告，let / const 會出錯"></a>var 允許重複宣告，let / const 會出錯</h2><p>最後再提一個能幫助防止開發錯誤或衝突的小地方，就是用 var 可以重複宣告同樣名稱的變數，而 let / const 如果重複宣告同名變數會出錯。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> myDogName = <span class="string">&#x27;吐司&#x27;</span>;</span><br><span class="line"><span class="keyword">var</span> myDogName = <span class="string">&#x27;厚片&#x27;</span>;</span><br><span class="line"><span class="built_in">console</span>.log(myDogName);</span><br><span class="line"><span class="comment">// 厚片</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> myDogName = <span class="string">&#x27;吐司&#x27;</span>;</span><br><span class="line"><span class="keyword">let</span> myDogName = <span class="string">&#x27;厚片&#x27;</span>;</span><br><span class="line"><span class="built_in">console</span>.log(myDogName);</span><br><span class="line"><span class="comment">// SyntaxError: Identifier &#x27;myDogName&#x27; has already been declared</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> myDogName = <span class="string">&#x27;吐司&#x27;</span>;</span><br><span class="line"><span class="keyword">const</span> myDogName = <span class="string">&#x27;厚片&#x27;</span>;</span><br><span class="line"><span class="built_in">console</span>.log(myDogName);</span><br><span class="line"><span class="comment">// SyntaxError: Identifier &#x27;myDogName&#x27; has already been declared</span></span><br></pre></td></tr></table></figure>

<p>這點對初學者或多人學做而言更能防止錯誤、容易尋找到錯誤所在。</p>
<br>
<hr>

<h2 id="var-與-let-const-差異總結"><a href="#var-與-let-const-差異總結" class="headerlink" title="var 與 let / const 差異總結"></a>var 與 let / const 差異總結</h2><p>啊啊不知不覺寫了頗長，但整理、學習的過程又讓再次了解、複習迴圈的細部運作與變量提升等等，算是蠻好玩的 !</p>
<p>內容比較多，在此三點總結：</p>
<ul>
<li>var 屬於函式作用域，let/const 屬於區域作用域，後者能避免更多情況下的同名變數與提取變數衝突、區塊內變數污染到全域的情況，且讓 for loop 使用更直覺方便。</li>
<li>var 會自動提升變數，let/const 則較為嚴謹，後者能避免忘記宣告變數或因無宣告讓變數污染到全域的情況。</li>
<li>var 能重複宣告同名變數，let/const 不能重複宣告同名變數，後者能避免些開發上的錯誤情況。</li>
</ul>
<p>綜合來說就是 let/const 將宣告變數變得更加嚴謹，藉此增加易讀性、防止出錯，而最重要的 CTA ，我想就是：<br>在 JS 中，不要再用 var 請都使用 let / const 宣告變數吧！</p>
]]></content>
      <tags>
        <tag>JavaScript</tag>
        <tag>Studies</tag>
      </tags>
  </entry>
  <entry>
    <title>【基礎教學】利用 Hexo 與 GitPage 建置個人 Blog</title>
    <url>/2021/09/09/teach-hexo/</url>
    <content><![CDATA[<p><img src="https://miro.medium.com/max/1400/0*wtxNrIzieLhNGPmP"></p>
<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>本文主要介紹如何使用 Hexo 來製作個人 Blog，並利用 GitPage 進行自動更新部署，讓沒接觸過新手小白也能輕鬆上手。</p>
<h2 id="環境建置"><a href="#環境建置" class="headerlink" title="環境建置"></a>環境建置</h2><p>在開始之前，須先：</p>
<ul>
<li>安裝 <a href="https://nodejs.org/en/">Node.js</a></li>
<li>安裝 <a href="https://git-scm.com/">Git</a></li>
<li>註冊 <a href="https://github.com/">GitHub</a> 帳號</li>
<li>瞭解 cmd 或 bash 指令（下文中所有以 ‘$’ 為開頭的，皆為 bash 指令）</li>
</ul>
<h3 id="建置-Hexo"><a href="#建置-Hexo" class="headerlink" title="建置 Hexo"></a>建置 Hexo</h3><h3 id="安裝-Hexo"><a href="#安裝-Hexo" class="headerlink" title="安裝 Hexo"></a>安裝 Hexo</h3><p>使用 npm 來安裝 hexo （須先安裝 Node.js）</p>
<figure class="highlight avrasm"><table><tr><td class="code"><pre><span class="line">$ npm install -g hexo-<span class="keyword">cli</span></span><br></pre></td></tr></table></figure>

<p>安裝成功後，輸入以下指令可查看版本。</p>
<figure class="highlight applescript"><table><tr><td class="code"><pre><span class="line">$ hexo <span class="built_in">version</span></span><br></pre></td></tr></table></figure>
<p><img src="https://miro.medium.com/max/1400/1*OUtcy0m_9ASKBBHJS__9jg.png"></p>
<h3 id="安裝-Hexo-Git"><a href="#安裝-Hexo-Git" class="headerlink" title="安裝 Hexo Git"></a>安裝 Hexo Git</h3><figure class="highlight ada"><table><tr><td class="code"><pre><span class="line">$ npm install hexo-deployer-git <span class="comment">--save</span></span><br></pre></td></tr></table></figure>

<p>接下來即可初始化我們的部落格了。</p>
<p>執行下方指令，Hexo 就會在指定資料夾 <folder> 新增所需的檔案。</folder></p>
<figure class="highlight dts"><table><tr><td class="code"><pre><span class="line">hexo init <span class="params">&lt;folder&gt;</span>   <span class="meta"># 初始化 blog 資料夾（自己命名）</span></span><br><span class="line">cd <span class="params">&lt;folder&gt;</span>          <span class="meta"># 移動到剛創建的 blog 資料夾裡</span></span><br><span class="line">npm install          <span class="meta"># 安裝相關套件</span></span><br></pre></td></tr></table></figure>

<p>安裝完成之後，docs 的目錄結構如下：</p>
<ul>
<li><code>_config.yml</code> 網站 全域配置 檔案，可以在此設置大部分的設定，其設定是用 yaml 格式編寫。</li>
<li><code>package.json</code> Node.js 的套件版本控制。</li>
<li><code>scaffolds</code> Hexo 釋出文章的時候使用，建立新文章時，會根據 scaffold 來建立檔案。</li>
<li><code>source</code> MarkDown 和各種原始檔，放置內容的地方。</li>
<li><code>themes</code> Hexo 的主題資料夾，會根據主題來產生靜態檔案。</li>
</ul>
<h3 id="本地端啟動"><a href="#本地端啟動" class="headerlink" title="本地端啟動"></a>本地端啟動</h3><figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line">$ hexo <span class="keyword">generate</span></span><br></pre></td></tr></table></figure>

<p>亦可用 <code>hexo g</code>，產生靜態檔案，會在目錄下產生 public 資料夾。</p>
<figure class="highlight axapta"><table><tr><td class="code"><pre><span class="line">$ hexo <span class="keyword">server</span></span><br></pre></td></tr></table></figure>

<p>亦可用 <code>hexo s</code>，啟動伺服器，預設是 <code>http://localhost:4000/</code></p>
<figure class="highlight elixir"><table><tr><td class="code"><pre><span class="line"><span class="variable">$ </span>hexo deploy</span><br></pre></td></tr></table></figure>

<p>亦可用 <code>hexo d</code>，部署網站。（ 如：github、heroku 等 ）</p>
<p>完成上述步驟後，在瀏覽器中即可看到本地端啟動的個人網頁了。</p>
<br>
<hr>

<h2 id="部署到-GitHub"><a href="#部署到-GitHub" class="headerlink" title="部署到 GitHub"></a>部署到 GitHub</h2><h3 id="新建-GitHub-Repository"><a href="#新建-GitHub-Repository" class="headerlink" title="新建 GitHub Repository"></a>新建 GitHub Repository</h3><p>接著，我們可以在 Github 的主分支上來建立我們的文件了。有兩種方式可以達成：</p>
<ul>
<li>在一個已存在的專案中建立文件</li>
<li>建立一個新的專案 <a href="https://github.com/new">Create a new repository</a></li>
</ul>
<p>簡單起見，假設我們新建立了一個名為 <code>yourname.github.io</code> 的倉庫（repository），當然你也可以用一個已經存在的專案繼續下面的操作。</p>
<p><img src="https://miro.medium.com/max/1400/1*-mmXEsYov4s11mpPSvu0yA.png"></p>
<p style="text-align:center"><i>請將上面的 yourname 替換成你 GitHub 的帳號名稱</i></p>

<p><img src="https://miro.medium.com/max/1400/1*cOqn6zvl5jWSGLqinnhuAw.png"></p>
<p style="text-align:center"><i>創建完新 repo 的畫面</i></p>

<p>再來，到已創立的指定資料夾 <folder>中找一個 <code>_config.yml</code> 的檔案。</folder></p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Site</span></span><br><span class="line">title: <span class="regexp">/標題(會顯示在網頁標題與頁首)/</span></span><br><span class="line">subtitle: <span class="regexp">/子標題(顯示在頁首)/</span></span><br><span class="line">description: <span class="regexp">/內容描述(給搜尋引擎看的)/</span></span><br><span class="line">author: <span class="regexp">/作者(顯示在頁尾)/</span></span><br><span class="line">language: <span class="regexp">/網站預設語言(台灣:zh-tw)/</span></span><br><span class="line">timezone: <span class="regexp">/時區(預設使用你電腦的時區)/</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># URL</span></span><br><span class="line">url: <span class="regexp">/網站的網域位址/</span></span><br><span class="line">root: <span class="regexp">/網站根目錄/</span></span><br><span class="line">permalink: <span class="regexp">/文章目錄(預設使用 YYYY\MM\DD\文章名稱)/</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Extensions</span></span><br><span class="line">theme: <span class="regexp">/網站的佈景主題/</span><span class="comment"># Deployment</span></span><br><span class="line">deploy:</span><br><span class="line"> type: <span class="regexp">/發佈型態/</span> </span><br><span class="line"> repository: <span class="regexp">/部署位置/</span> </span><br><span class="line"> branch: <span class="regexp">/分支/</span></span><br><span class="line"> message: <span class="regexp">/部署訊息/</span></span><br></pre></td></tr></table></figure>

<p>開啟之後，拉到檔案最底部，可以看到</p>
<figure class="highlight dts"><table><tr><td class="code"><pre><span class="line"><span class="symbol">deploy:</span></span><br><span class="line"><span class="symbol"> type:</span></span><br></pre></td></tr></table></figure>

<p>將以下資訊對應輸入</p>
<figure class="highlight dts"><table><tr><td class="code"><pre><span class="line"><span class="symbol">deploy:</span></span><br><span class="line"><span class="symbol"> type:</span> git</span><br><span class="line"><span class="symbol"> repository:</span> http:<span class="comment">//github.com/yourname/yourname.github.io.git</span></span><br><span class="line"><span class="symbol"> branch:</span> master</span><br></pre></td></tr></table></figure>

<blockquote>
<p><b>注意：</b><br>設定中的 : 後一定要有一個空格<br>將上面的 yourname 改成你的 GitHub 帳號名稱</p>
</blockquote>
<br>

<h3 id="連線上傳-GitHub"><a href="#連線上傳-GitHub" class="headerlink" title="連線上傳 GitHub"></a>連線上傳 GitHub</h3><blockquote>
<p>過程中，可能會需要輸入GitHub的帳號、密碼做授權。</p>
</blockquote>
<p>使用下方的命令在本地 clone 專案：</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line">$ git clone http:<span class="regexp">//gi</span>thub.com<span class="regexp">/yourname/y</span>ourname.github.io.git</span><br></pre></td></tr></table></figure>

<p>將 <code>USERNAME</code> 替換為你的使用者名稱，<code>REPOSITORY</code> 替換為你的專案名稱。</p>
<figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">$ git <span class="builtin-name">add</span> .</span><br></pre></td></tr></table></figure>

<p>為 commit 留註解。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">$ git <span class="keyword">commit</span> <span class="operator">-</span>m “<span class="keyword">first</span> <span class="keyword">commit</span>”</span><br></pre></td></tr></table></figure>

<p>手動新增 GitHub 遠端 branch 名稱、連結</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line">$ git remote origin  http:<span class="regexp">//gi</span>thub.com<span class="regexp">/yourname/y</span>ourname.github.io.git</span><br></pre></td></tr></table></figure>

<p>推到你的 GitHub master。</p>
<figure class="highlight maxima"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">push</span> <span class="built_in">origin</span> master</span><br><span class="line"># 只有第一次 <span class="built_in">push</span> 需要全打，第二次以後執行 git <span class="built_in">push</span> 即可</span><br></pre></td></tr></table></figure>

<p>這樣就把本地的檔案上傳到 GitHub 囉！</p>
<h3 id="設定-GitPage"><a href="#設定-GitPage" class="headerlink" title="設定 GitPage"></a>設定 GitPage</h3><p>打開 GitHub 專案的 Setting，下面有個 GitHub Page 的地方，將其勾選設定，接著就可以在你個人的 <a href="https://yourname.github.io/">https://yourname.github.io/</a> 看到畫面了。</p>
<p><img src="https://miro.medium.com/max/1400/1*fbLuA1K6Vik-ezAiUbAuJA.jpeg"></p>
<br>

<h2 id="設定專屬個人-Domain"><a href="#設定專屬個人-Domain" class="headerlink" title="設定專屬個人 Domain"></a>設定專屬個人 Domain</h2><p>有了個人的 GitHub Page 後，若想把網址換成專屬網域名稱，提供以下兩個網站可供參考：</p>
<ol>
<li>Gandi.net</li>
<li>Godaddy</li>
</ol>
<p>進去網站後，挑選並確定了自己想購買的網址後，在購物車結帳頁面可以選擇自己想要購買的年限，至少要買一年，若一年後還要再使用可以自行續約。</p>
<p>以 Gandi.net 為例，購買後，到 域名－區域檔紀錄－新增</p>
<p><img src="https://miro.medium.com/max/1400/1*Yjbd4zMuUMw2n56nJysL-Q.png"></p>
<p style="text-align:center"><i>新增紅框中的四筆 DNS</i></p>

<p>新增 DNS 紀錄 （可參考 <a href="https://docs.github.com/en/github/working-with-github-pages/managing-a-custom-domain-for-your-github-pages-site">GitHub Page 官方說明文件</a></p>
<figure class="highlight accesslog"><table><tr><td class="code"><pre><span class="line">類型： A</span><br><span class="line">TTL： <span class="number">1800</span></span><br><span class="line">IPv4： </span><br><span class="line"><span class="number">185.199.108.153</span></span><br><span class="line"><span class="number">185.199.109.153</span></span><br><span class="line"><span class="number">185.199.110.153</span></span><br><span class="line"><span class="number">185.199.111.153</span></span><br></pre></td></tr></table></figure>

<p>再來，到 GitHub 的 Setting，Custom Domain 欄位輸入購買的網址，按儲存就設定完成了。</p>
<blockquote>
<p>小建議，下方的 HTTPS 也一同勾選。</p>
</blockquote>
<p><img src="https://miro.medium.com/max/1400/1*895UBYgeMPzh1OjOyQlgRA.png"></p>
<p>稍待一陣子，就可以用新購買的網址進入個人網站囉。</p>
<hr>
<br>

<h2 id="更換主題"><a href="#更換主題" class="headerlink" title="更換主題"></a>更換主題</h2><p>Hexo 預設的主題是 <a href="https://github.com/hexojs/hexo-theme-landscape">landscape</a>，而我們可以到 <a href="https://hexo.io/themes/">Hexo主題庫</a> 來挑選我們欲安裝的主題，以下以 <a href="https://github.com/zhwangart/hexo-theme-ocean">Ocean</a> 為例。</p>
<h3 id="安裝-Ocean"><a href="#安裝-Ocean" class="headerlink" title="安裝 Ocean"></a>安裝 Ocean</h3><p>安裝 Ocean</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line">$ git clone https:<span class="regexp">//gi</span>thub.com<span class="regexp">/zhwangart/</span>hexo-theme-ocean.git themes/ocean</span><br></pre></td></tr></table></figure>

<p>修改網站設定 <code>&lt;folder&gt;/ _config.yml</code></p>
<figure class="highlight avrasm"><table><tr><td class="code"><pre><span class="line"><span class="symbol">theme:</span> ocean</span><br></pre></td></tr></table></figure>

<p>在 <code>&lt;folder&gt;/theme/ocean/</code> 裡面的 <code>_config.yml</code> 也可以自己設定</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Menu</span></span><br><span class="line"><span class="attr">menu:</span></span><br><span class="line">  <span class="attr">Home:</span> <span class="string">/</span></span><br><span class="line">  <span class="attr">Archives:</span> <span class="string">/archives</span></span><br><span class="line">  <span class="attr">Gallery:</span> <span class="string">/gallery</span></span><br><span class="line">  <span class="attr">About:</span> <span class="string">/about</span></span><br><span class="line">  <span class="attr">Links:</span> <span class="string">/links</span></span><br><span class="line"><span class="attr">rss:</span> <span class="string">/atom.xml</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Miscellaneous</span></span><br><span class="line"><span class="attr">favicon:</span> <span class="string">/favicon.ico</span></span><br><span class="line"><span class="attr">brand:</span> <span class="string">/images/hexo.svg</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Ocean Video</span></span><br><span class="line"><span class="comment"># Because I put videos in multiple formats on the same path, I just labeled the path here.</span></span><br><span class="line"><span class="attr">ocean:</span></span><br><span class="line">  <span class="attr">overlay:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">path:</span> <span class="string">images/ocean/</span>      <span class="comment"># Video storage path, formats: mp4/ogg/webm</span></span><br><span class="line">  <span class="attr">brand:</span> <span class="string">/images/hexo-inverted.svg</span>      <span class="comment"># Optional, a small logo</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Content</span></span><br><span class="line"><span class="attr">excerpt_link:</span> <span class="string">Read</span> <span class="string">More...</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># fancybox</span></span><br><span class="line"><span class="attr">fancybox:</span> <span class="literal">true</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Local search</span></span><br><span class="line"><span class="attr">search_text:</span> <span class="string">Search</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Gitalk</span></span><br><span class="line"><span class="attr">gitalk:</span></span><br><span class="line">  <span class="attr">enable:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">clientID:</span> <span class="comment"># GitHub Application Client ID</span></span><br><span class="line">  <span class="attr">clientSecret:</span> <span class="comment"># Client Secret</span></span><br><span class="line">  <span class="attr">repo:</span> <span class="comment"># Repository name</span></span><br><span class="line">  <span class="attr">owner:</span> <span class="comment"># GitHub ID</span></span><br><span class="line">  <span class="attr">admin:</span> <span class="comment"># GitHub ID</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Valine</span></span><br><span class="line"><span class="attr">valine:</span></span><br><span class="line">  <span class="attr">enable:</span> <span class="literal">false</span>    <span class="comment"># Default: false.</span></span><br><span class="line">  <span class="attr">el:</span> <span class="string">&#x27;vcomments&#x27;</span>    <span class="comment"># The DOM element to be mounted on initialization.</span></span><br><span class="line">  <span class="attr">appId:</span>    <span class="comment"># Application appId from Leancloud.</span></span><br><span class="line">  <span class="attr">appKey:</span>    <span class="comment"># Application appKey from Leancloud.</span></span><br><span class="line">  <span class="attr">notify:</span> <span class="literal">false</span>    <span class="comment"># Mail notifier, Default: false.</span></span><br><span class="line">  <span class="attr">verify:</span> <span class="literal">true</span>    <span class="comment"># Validation code, Default: true.</span></span><br><span class="line">  <span class="attr">avatar:</span> <span class="string">&#x27;mp&#x27;</span>    <span class="comment"># Gravatar type.</span></span><br><span class="line">  <span class="attr">pageSize:</span> <span class="string">&#x27;10&#x27;</span>    <span class="comment"># Number of pages per page.</span></span><br><span class="line">  <span class="attr">placeholder:</span> <span class="string">&#x27;請輸入...&#x27;</span>    <span class="comment">#</span></span><br></pre></td></tr></table></figure>

<p>重新啟動 server</p>
<figure class="highlight axapta"><table><tr><td class="code"><pre><span class="line">$ hexo <span class="keyword">server</span></span><br></pre></td></tr></table></figure>

<p>這樣就更改成功了！</p>
<p><img src="https://miro.medium.com/max/1400/1*0Ji-3ir8nokLgZGvtzKVIw.png"></p>
<p style="text-align:center"><i>修改後的 Ocean 主題</i></p>

<br>
<hr>

<h2 id="發佈文章"><a href="#發佈文章" class="headerlink" title="發佈文章"></a>發佈文章</h2><figure class="highlight gauss"><table><tr><td class="code"><pre><span class="line">$ hexo <span class="keyword">new</span> [layout] &lt;<span class="built_in">title</span>&gt;</span><br></pre></td></tr></table></figure>

<p>你可以在指令中指定文章的佈局（layout），預設為 post ，也可以透過修改 <code>_config.yml</code> 中的 <code>default_layout</code> 設定來指定預設佈局。</p>
<h3 id="佈局（Layout）"><a href="#佈局（Layout）" class="headerlink" title="佈局（Layout）"></a>佈局（Layout）</h3><p>Hexo 有三種預設佈局：post、page 和 draft，它們分別對應不同的路徑，而你所自定的其他佈局和 post 相同，都儲存至 source/_posts 資料夾。</p>
<p><img src="https://miro.medium.com/max/824/1*eFVpwYyoIyZZ2x19f8r8Pw.png"></p>
<figure class="highlight haxe"><table><tr><td class="code"><pre><span class="line">$ hexo <span class="keyword">new</span> <span class="type"></span>“postName”         <span class="meta"># 新建文章</span></span><br><span class="line">$ hexo <span class="keyword">new</span> <span class="type">page</span> “pageName”    <span class="meta"># 新建頁面</span></span><br><span class="line">$ hexo publish “draftName”    <span class="meta"># 新建草稿</span></span><br></pre></td></tr></table></figure>

<h3 id="刪除文章"><a href="#刪除文章" class="headerlink" title="刪除文章"></a>刪除文章</h3><p>首先進入到 <code>source /_post</code> 資料夾中，找到 <code>helloworld.md</code>，在本地直接執行刪除。</p>
<p><img src="https://miro.medium.com/max/1400/0*q6PVwd41rT6-e47s.png"></p>
<p>執行以下操作，一段時間後就會發現文章已刪除。</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line">$ hexo clean       # 清除快取檔案和已產生的靜態檔案。</span><br><span class="line">$ hexo d -g        # d = deploy, g = <span class="keyword">generate</span></span><br></pre></td></tr></table></figure>

<h3 id="其他指令"><a href="#其他指令" class="headerlink" title="其他指令"></a>其他指令</h3><figure class="highlight vala"><table><tr><td class="code"><pre><span class="line">$ hexo server -p <span class="number">5000</span></span><br><span class="line"><span class="meta"># 如果 port:4000 被佔用，可以用這個$ hexo render &lt;file1&gt; [file2] </span></span><br><span class="line"><span class="meta"># 將文件渲染成靜態頁面</span></span><br></pre></td></tr></table></figure>

<p>附錄：<a href="https://hexo.io/docs/">Hexo 官方參考文件</a></p>
]]></content>
      <tags>
        <tag>教學</tag>
        <tag>Hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>TailwindCSS</title>
    <url>/2022/01/19/Tailwind/</url>
    <content><![CDATA[<hr>
<h2 id="date-2022-01-1915-54"><a href="#date-2022-01-1915-54" class="headerlink" title="date: 2022-01-1915:54"></a>date: 2022-01-1915:54</h2><h6 id="Tags-TailwindCSS-css"><a href="#Tags-TailwindCSS-css" class="headerlink" title="Tags: #TailwindCSS #css"></a>Tags: #TailwindCSS #css</h6><h1 id="初學-TailwindCSS"><a href="#初學-TailwindCSS" class="headerlink" title="初學 TailwindCSS"></a>初學 TailwindCSS</h1><p>正好想學，在研究之餘，順便寫個教學留個紀錄。</p>
<h2 id="Tailwind-安裝流程-（使用-npm）"><a href="#Tailwind-安裝流程-（使用-npm）" class="headerlink" title="Tailwind 安裝流程 （使用 npm）"></a>Tailwind 安裝流程 （使用 npm）</h2><p>依照 <a href="https://tailwindcss.com/docs/installation">Tailwind 的官網</a>，安裝有兩種方式：</p>
<ul>
<li>使用 CDN</li>
<li>使用 npm </li>
</ul>
<p>而今天主要是以 npm 的方式來展示。</p>
<p>因為 Tailwind 若是靠 CDN 的方式，會導致很多強大的功能沒有全開，我們這邊就簡單舉例幾個：</p>
<ul>
<li>基本的，就是無法 <strong>新增自訂顏色</strong> 跟 <strong>支援深色模式</strong>。  </li>
<li>再進階一點，就是無法使用 <strong><code>`@apply`</code></strong> 指令將語法整理起來。  </li>
<li>最嚴重的，就是 <strong>無法壓縮檔案</strong>，CDN 版大小約 2.79MB。 （完整版約 3.7MB~4MB）</li>
</ul>
<p>所以如果只是為了 <strong>玩玩、體驗 Tailwind 的特性及帶來的方便性</strong>，完全可以<strong>使用 CDN</strong> 的方式就好，但如果是想要做公司產品／成品上線，這個沒壓縮的大小很有可能讓你被火了XDDD</p>
<h2 id="前置作業-安裝-node-js"><a href="#前置作業-安裝-node-js" class="headerlink" title="前置作業 - 安裝 node.js"></a>前置作業 - 安裝 node.js</h2><p>首先，如果你會用 linux 類系統，這個你肯定用過。<br>不過新手們的話就別跳過，還是都跑一遍吧，以免你有指令沒跑到或東西沒裝到。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt update</span><br></pre></td></tr></table></figure>

<p>接著，這個很重要，要安裝 curl。 因為等一下就要靠他了。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt-get install curl</span><br></pre></td></tr></table></figure>

<p>再來，使用到剛剛安裝的 curl，把 node.js 12版的安裝腳本下載下來。請注意，這邊一定要抓12版或更新的！</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">curl -sL https://deb.nodesource.com/setup_12.x | sudo -E bash -</span><br></pre></td></tr></table></figure>

<p>這下，就可以放心的把 node.js 安裝下去了~</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt-get install nodejs</span><br></pre></td></tr></table></figure>


<h2 id="安裝-Tailwind"><a href="#安裝-Tailwind" class="headerlink" title="安裝 Tailwind"></a>安裝 Tailwind</h2><blockquote>
<p>注意：Tailwind CSS 需要 Node.js 版本在 12.13.0 以上</p>
</blockquote>
<ol>
<li>首先，因為 Thailwind 有 node.js 的版本限制，可以先執行以下的指令檢查 node.js 的版本。</li>
</ol>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">node --version</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>如果檢查完，確定 node 是 12 版以上，那就可以放心地執行下面這行指令了。</li>
</ol>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm init</span><br></pre></td></tr></table></figure>

<p>在打入 <code>NPM init</code> 後，會被要求輸入幾個欄位</p>
<blockquote>
<p><code>package name</code>: 你這個 Project 要叫什麼名字<br><code>version</code>: 你決定這個 Project 現在該是第幾版<br><code>description</code>: Project 基本介紹<br><code>entry point</code>: 進入點，如果要跑你的 Project 應該要執行哪個檔案<br><code>author</code>: 作者（自己）<br><code>license</code>: 你這個 Project 是採用什麼授權的<br><code>test command</code>: 這個不太重要，待會會說明</p>
</blockquote>
<p>基本上結束後，你可以看到這個資料夾底下，新增了一個 <code>Package.json</code></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="string">&quot;name&quot;</span>: <span class="string">&quot;my-project&quot;</span>,</span><br><span class="line">  <span class="string">&quot;version&quot;</span>: <span class="string">&quot;1.0.0&quot;</span>,</span><br><span class="line">  <span class="string">&quot;description&quot;</span>: <span class="string">&quot;this is my project&quot;</span>,</span><br><span class="line">  <span class="string">&quot;main&quot;</span>: <span class="string">&quot;app.js&quot;</span>,</span><br><span class="line">  <span class="string">&quot;scripts&quot;</span>: &#123;</span><br><span class="line">    <span class="string">&quot;test&quot;</span>: <span class="string">&quot;echo \&quot;Error: no test specified\&quot; &amp;&amp; exit 1&quot;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="string">&quot;author&quot;</span>: <span class="string">&quot;Kimi&quot;</span>,</span><br><span class="line">  <span class="string">&quot;license&quot;</span>: <span class="string">&quot;ISC&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>基本上使用 NPM 來創建的 Project，他會連一些專案的資訊都幫你做管理。</p>
<blockquote>
<p><strong>Optional Info</strong>：<br>如果你覺得上面要一直輸入很冗，可以使用  <code>npm init -y</code> 。<br>他跟  <code>npm init</code>  幾乎一樣，只是它會幫你把預設選項全部跳過，產生一個空白的 <code>package.json</code> （懶人專用）</p>
</blockquote>
<ol start="3">
<li>接著， 依照官方教學，需安裝 Tailwind CSS、PostCSS，還有 autoprefixer 這三個檔案；因為 Tailwind 不會自動加上瀏覽器前缀詞到產生的 CSS 中，所以瀏覽器無法讀取 CSS，要加上 autoprefixer 這個套件去處理此問題。<br> 我們使用一行指令就三個安裝好了，輕鬆愉快。</li>
</ol>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm install tailwindcss@latest postcss@latest autoprefixer@latest</span><br></pre></td></tr></table></figure>

<ol start="4">
<li>再來，為了以防萬一，我們執行一次 <code>`npm install`</code> 來更新和安裝其他依賴套件的狀態。</li>
</ol>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm install</span><br></pre></td></tr></table></figure>

<p>那這樣就算安裝完了，只是距離使用，中間還有一些東西需要設定。<br>如果你還會透過 postCSS 安裝其他插件、或是像官方的推薦使用 autoprefixer的話，可以看一下下方的 **安裝 PostCSS 套件。</p>
<h3 id="Optional-Info"><a href="#Optional-Info" class="headerlink" title="Optional Info"></a>Optional Info</h3><p>如果在安裝過程中，有遇到以下錯誤，那可能是 <strong>將 Tailwind 與舊版本的 PostCSS 混合使用</strong> 所導致的結果， v2.0 版本的 Tailwind 依賴於 PostCSS 8。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">Error: PostCSS plugin tailwindcss requires PostCSS 8.</span><br></pre></td></tr></table></figure>

<p>這時請安裝 <a href="https://www.tailwindcss.cn/docs/installation#post-css-7">PostCSS 7</a> 版本，才能兼容。</p>
<hr>

<h4 id="安裝-PostCSS-7"><a href="#安裝-PostCSS-7" class="headerlink" title="安裝 PostCSS 7"></a>安裝 PostCSS 7</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm uninstall tailwindcss</span><br><span class="line">npm install tailwindcss@npm:@tailwindcss/postcss7-compat</span><br></pre></td></tr></table></figure>

<hr>

<h4 id="安裝-PostCSS-套件"><a href="#安裝-PostCSS-套件" class="headerlink" title="安裝 PostCSS 套件"></a>安裝 PostCSS 套件</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm install tailwindcss autoprefixer postcss postcss-cli</span><br></pre></td></tr></table></figure>

<p>上面這個指令，就是幫你的專案透過 npm 安裝 tailwindcss、postcss、autoprefixer，及 postcss-cli 這四個東西。<br>那如果有看過官方教學，對前三個東西應該不陌生，只是最後一個也是必須的，算是幫官方安裝過程的補充。</p>
<p>在前面的步驟產生完 Tailwind 設定檔之後，我們要來產生 PostCSS 的設定檔，不然 PostCSS 不會幫我們載入 tailwind 和 autoprefixer。</p>
<ul>
<li>  <strong>Windows用戶</strong></li>
</ul>
<figure class="highlight clean"><table><tr><td class="code"><pre><span class="line">在專案目錄下新增檔案 -&gt; 檔名為 <span class="string">&quot;postcss.config.js&quot;</span></span><br></pre></td></tr></table></figure>

<ul>
<li>  <strong>Linux、Ubuntu、MacOS 類用戶</strong>，直接下指令即可（ 在專案目錄下）</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">touch postcss.config.js</span><br></pre></td></tr></table></figure>

<p>新增完 <code>`postcss.config.js`</code>，我們要在檔案中加入下面的內容：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// postcss.config.js</span></span><br><span class="line"><span class="built_in">module</span>.exports = &#123;</span><br><span class="line">  <span class="attr">plugins</span>: [</span><br><span class="line">    <span class="built_in">require</span>(<span class="string">&#x27;tailwindcss&#x27;</span>),</span><br><span class="line">    <span class="built_in">require</span>(<span class="string">&#x27;autoprefixer&#x27;</span>),</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>或是用官方給的格式也可以~</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// postcss.config.js</span></span><br><span class="line"><span class="built_in">module</span>.exports = &#123;</span><br><span class="line">  <span class="attr">plugins</span>: &#123;</span><br><span class="line">    <span class="attr">tailwindcss</span>: &#123;&#125;,</span><br><span class="line">    <span class="attr">autoprefixer</span>: &#123;&#125;,</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>最後的最後，只要把前面設定執行的地方，改成</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">scripts:[</span><br><span class="line">    <span class="comment">/* ... 前面是你其它的設定值，略 .... */</span></span><br><span class="line">    <span class="string">&quot;&#123; 你的執行名稱 &#125;&quot;</span>: <span class="string">&quot;postcss &#123;你 css 檔擺放的路徑&#125;/&#123;要寫樣式的css檔名稱&#125; -o &#123;你 css 檔擺放的路徑&#125;/&#123;頁面要掛載的css檔名稱&#125;&quot;</span></span><br><span class="line">],</span><br></pre></td></tr></table></figure>

<p>剩下的步驟，都跟上面 tailwind 的教學一樣囉~ </p>
<h2 id="設定-Tailwind"><a href="#設定-Tailwind" class="headerlink" title="設定 Tailwind"></a>設定 Tailwind</h2><ol>
<li>依照上方的流程，安裝完 Tailwind 應該會看到 <code>package.json</code> 中有新增後來安裝的 Tailwind CSS、PostCSS 和 autoprefixer。</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// package.json</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">&quot;dependencies&quot;</span>: &#123;</span><br><span class="line">    <span class="string">&quot;autoprefixer&quot;</span>: <span class="string">&quot;^10.2.6&quot;</span>,</span><br><span class="line">    <span class="string">&quot;postcss&quot;</span>: <span class="string">&quot;^8.3.0&quot;</span>,</span><br><span class="line">    <span class="string">&quot;tailwindcss&quot;</span>: <span class="string">&quot;^2.1.4&quot;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>在使用 Tailwind CSS 之前，我們得產生 tailwind 必要的設定檔，這也是要達到 tailwind 客製化前最重要的一步。</li>
</ol>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npx tailwindcss init</span><br></pre></td></tr></table></figure>

<p>執行上方的指令後，在專案目錄下應該會多出一個叫做 <code>`tailwind.config.js`</code> 的檔案。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">module</span>.exports = &#123;</span><br><span class="line">  <span class="attr">purge</span>: [],</span><br><span class="line">  <span class="attr">darkMode</span>: <span class="literal">false</span>, <span class="comment">// or &#x27;media&#x27; or &#x27;class&#x27;</span></span><br><span class="line">  <span class="attr">theme</span>: &#123;</span><br><span class="line">    <span class="attr">extend</span>: &#123;&#125;,</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">variants</span>: &#123;</span><br><span class="line">    <span class="attr">extend</span>: &#123;&#125;,</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">plugins</span>: [],</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>這麼一來，就可以自訂專屬於你的 Tailwind 設定了！ </p>
<h2 id="新增-Tailwind-到-CSS-中"><a href="#新增-Tailwind-到-CSS-中" class="headerlink" title="新增 Tailwind 到 CSS 中"></a>新增 Tailwind 到 CSS 中</h2><p>現在需要在你存放 CSS 樣式檔的資料夾裡，建立兩個 CSS 檔：</p>
<ul>
<li> 一個是你頁面要掛載的 <code>style.css</code> （名稱可自訂）</li>
<li>一個是用來寫 CSS 樣式的 <code>tailwind.css</code> （名稱也可自訂）</li>
</ul>
<p>這樣講可能有點抽象，拿我的專案目錄來舉例好了。</p>
<p><img src="https://i.imgur.com/SLrk4vo.png"></p>
<p>在我的專案下，擺放 CSS 檔的路徑是 <code>/public/css</code> ，那就在這個資料夾底下產生兩個 CSS檔，名稱自訂，我這邊是把要掛載的叫做 <code>app.css</code>，要寫樣式內容的叫做 <code>tailwind.css</code>。</p>
<p>建立完之後，我們在要用來寫樣式的 CSS 檔 （我這邊叫做 <code>tailwind.css</code> ）裡面貼上下面的 CSS 語法。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 貼在用來寫樣式的css檔，範例這邊是 tailwind.css */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">@tailwind</span> base; </span><br><span class="line"><span class="keyword">@tailwind</span> components; </span><br><span class="line"><span class="keyword">@tailwind</span> utilities;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>我們要掛載的 CSS 檔 (這邊叫做 <code>app.css</code>) 保持空白就好，以後所有要補充的樣式都寫在另一個 CSS 檔（<code>tailwind.css</code>）裡。<br>因為如果接下來 <strong>進行編譯</strong>， 要掛載的 CSS 檔 (<code>app.css</code>) 的 <strong>內容會被覆蓋掉</strong>。</p>
</blockquote>
<h2 id="編譯"><a href="#編譯" class="headerlink" title="編譯"></a>編譯</h2><p>終於進入最後一步了。<br>開啟剛剛 <code>`npm init`</code> 執行後產生的 <code>package.json</code>，在 scripts 裡面加一行：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">scripts:[</span><br><span class="line">    <span class="comment">/* ... 前面是你其它的設定值，略 .... */</span></span><br><span class="line">    <span class="string">&quot;&#123; 你的執行名稱 &#125;&quot;</span>: <span class="string">&quot;tailwind build &#123;你 css 檔擺放的路徑&#125;/&#123;要寫樣式的css檔名稱&#125; -o &#123;你 css 檔擺放的路徑&#125;/&#123;頁面要掛載的css檔名稱&#125;&quot;</span></span><br><span class="line">],</span><br></pre></td></tr></table></figure>

<p>如果把上述的名稱都取代完之後，在我這邊的範例，會長的像下面這樣：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">scripts:[</span><br><span class="line">    <span class="comment">/* ... 前面是你其它的設定值，略 .... */</span></span><br><span class="line">    <span class="string">&quot;build-css&quot;</span>: <span class="string">&quot;tailwind build public/css/tailwind.css -o public/css/app.css&quot;</span></span><br><span class="line">],</span><br></pre></td></tr></table></figure>

<p>最後，我們只要執行 <code>npm run &#123;你的執行名稱&#125;</code>，那他就會開始進行編譯了！</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm run build-css</span><br></pre></td></tr></table></figure>

<p>編譯完成後，應該會在命令列 (終端機) 看到下面的訊息： </p>
<p><img src="https://i.imgur.com/vYO0NIn.png"></p>
<p>這樣就算編譯完成了，現在只要你在頁面掛上要掛載的 CSS 檔，並用上 Tailwind 語法，就會有效果囉！</p>
<h2 id="使用-Tailwind-CSS"><a href="#使用-Tailwind-CSS" class="headerlink" title="使用 Tailwind CSS"></a>使用 Tailwind CSS</h2><p>在你網頁中插入的 CSS 檔路徑要改成剛剛上面說過的，用來掛載在頁面上的 CSS 檔。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 你專案中要使用 Tailwind CSS 的頁面 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 前略 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">&quot;stylesheet&quot;</span> <span class="attr">href</span>=<span class="string">&quot;./&#123;你CSS檔的路徑&#125;/&#123;要掛載在頁面的CSS檔名稱&#125;&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 略 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>底下這邊是我依照我專案所改寫後的。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 這範例是我專案中的 index.html --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 前略 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">&quot;stylesheet&quot;</span> <span class="attr">href</span>=<span class="string">&quot;./css/app.css&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 馬上用 Tailwind 寫一個熱騰騰的黃綠藍三色漸層 Hello World 方塊 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;m-3 p-10 bg-gradient-to-r from-yellow-400 via-green-400 to-blue-400 text-white&quot;</span>&gt;</span></span><br><span class="line">          Hello World ! I&#x27;m using Tailwind CSS Now ~</span><br><span class="line">        <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>這樣就把 Tailwind CSS 安裝完了，這樣你就可以盡情地使用 Tailwind 寫出一堆美美的介面了啦！</p>
<h2 id="Optional：幫-CSS-瘦身"><a href="#Optional：幫-CSS-瘦身" class="headerlink" title="Optional：幫 CSS 瘦身"></a>Optional：幫 CSS 瘦身</h2><p>Tailwind 因為是 Utility 的關係，會把整包檔案匯入，如果整包檔案要選染於網頁，效能就會低落。</p>
<p>Tailwind 官方文件有提供可以瘦身的方法：</p>
<ol>
<li>選到 <code>tailwind.config.js</code> 檔案，將我可能會使用到的內容寫在  <code>purge</code> 屬性內</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//tailwind.config.js</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">module</span>.exports = &#123;</span><br><span class="line">  <span class="attr">purge</span>: &#123;</span><br><span class="line">    <span class="attr">enabled</span>: <span class="literal">true</span>,</span><br><span class="line">    <span class="attr">content</span>: [<span class="string">&quot;./**/*.html&quot;</span>],</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">darkMode</span>: <span class="literal">false</span>, <span class="comment">// or &#x27;media&#x27; or &#x27;class&#x27;</span></span><br><span class="line">  <span class="attr">theme</span>: &#123;</span><br><span class="line">    <span class="attr">extend</span>: &#123;&#125;,</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">variants</span>: &#123;</span><br><span class="line">    <span class="attr">extend</span>: &#123;&#125;,</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">plugins</span>: [],</span><br><span class="line">&#125;;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<ol start="2">
<li>這樣程式執行時，就會去監聽我哪支檔案有使用到，只會編譯使用的檔案，真的很方便！</li>
</ol>
]]></content>
      <tags>
        <tag>教學</tag>
        <tag>技術文件</tag>
        <tag>TailwindCSS</tag>
      </tags>
  </entry>
</search>
